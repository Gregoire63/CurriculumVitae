Bundle de la branche addSportSection (9 commits, non poussés — l'environnement
distant est en lecture seule, git ET l'API GitHub renvoient 403).

À FAIRE de ton côté pour synchroniser la branche :
1. Copier tous les fichiers de ce dossier à la racine du repo (mêmes chemins).
2. SUPPRIMER le fichier obsolète :  git rm sportProgram.ts
   (il a été déplacé dans data/sportProgram.ts)
3. git add -A && git commit && git push origin addSportSection

Changement de ce tour : navigation en bas sur mobile/tablette, en haut sur
desktop (fichier pages/sport.vue).
